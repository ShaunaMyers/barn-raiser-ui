import { needsData } from "../../needsData";

const Need = ({ id, pointOfContact, title, description, startTime, endTime, zipCode, supporters }) => {
    return ( 
        <div className="need-container">
            <h4>Hello</h4>
        </div>
     );
}
 
export default Need;